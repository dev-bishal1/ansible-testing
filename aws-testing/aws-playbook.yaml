- name: AWS related configuration
  hosts: localhost
  gather_facts: False
  tasks:
    - name: create a new ec2 key pair, returns generated private key
      ec2_key:
        name: test-key-pair
        region: us-east-1
      register: key-test

    - name: Debug the generated key-test
      debug:
        var: key-test
